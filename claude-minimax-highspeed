#!/usr/bin/env bash
set -euo pipefail

# claude-minimax-highspeed — launch Claude Code with MiniMax-M2.1-Fusion (optimized variant)
# All other settings identical to claude-minimax; only the model differs.

exec "$HOME/.local/bin/claude-minimax" "$@"
